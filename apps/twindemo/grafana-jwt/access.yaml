apiVersion: twingate.com/v1beta
kind: TwingateResourceAccess
metadata:
  name: grafana-jwt-access
  namespace: monitoring
spec:
  resourceRef:
    name: grafana-jwt-resource
    namespace: monitoring
  principalExternalRef:
    type: group
    name: Admins
