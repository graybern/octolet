apiVersion: twingate.com/v1beta
kind: TwingateResourceAccess
metadata:
  name: grafana-basic-access
  namespace: default
spec:
  resourceRef:
    name: grafana-basic-resource
    namespace: default
  principalExternalRef:
    type: group
    name: Admins
